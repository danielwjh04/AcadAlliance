# AcadAlliance
Python-based study group matching engine and Telegram bot interface. Uses a weighted scoring algorithm to pair students based on exact course matches, time availability, and study goals.
